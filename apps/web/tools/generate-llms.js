#!/usr/bin/env node
// Placeholder script to satisfy build step when `generate-llms.js` is missing.
// It intentionally does nothing and exits successfully.
console.log('generate-llms placeholder: no-op');
process.exit(0);
